spring:
  application:
    name: Poppy-Server
poppy:
  agent:
    heartbeat-timeout-seconds: ${POPPY_AGENT_HEARTBEAT_TIMEOUT_SECONDS:90}
    heartbeat-scan-interval-milliseconds: ${POPPY_AGENT_HEARTBEAT_SCAN_INTERVAL_MILLISECONDS:30000}
