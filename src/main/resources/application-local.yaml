spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:poppy}
    username: ${DB_USERNAME:poppy}
    password: ${DB_PASSWORD:poppy}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
poppy:
  agent:
    token: ${POPPY_AGENT_TOKEN:}
